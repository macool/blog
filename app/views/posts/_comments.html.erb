<%= pluralize @post.approved_comments_count, t("comment", default: "comment") %>

<% comments.each do |comment| %>
  <div class="comment">
    <%= render 'posts/comment', comment: comment %>
  </div>
<% end %>
