$(".post_comments").append( $("<div />", {class: "comment", html: "<%= j render('posts/comment', comment: @comment) %>"}) );
$(".post_new_comment_form").slideUp("fast");
Helpers.AlertsHelper.create({ kind: "success", content: "<%= t("comments.created_and_waiting", default: "Comment pending approval") %>" });
